name: ZAP

on:
  workflow_dispatch:

jobs:
  docker:
    runs-on: ubuntu-latest
    name: Docker build

    steps:
      - name: ZAP Scan
        uses: zaproxy/action-baseline@v0.13.0
        with:
          target: "https://www.mvdam.dev"
